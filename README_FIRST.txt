WIND SIGHT PRS — v3.8.10 Competition Core | ZealTech
==================================================
APPLICATION PACKAGE — deploy this ZIP's extracted root files to the EXISTING
static app project/URL. This is not a WordPress plugin.

Automated engineering gate: 220/220 named checks passed in each of two full runs
on identical runtime bytes. Physical iPhone/Safari/PWA, hosted update, native
storage/offline durability and real-world ballistic acceptance remain unverified.
Read QA_REPORT_v3.8.10.txt for the scope and device checklist. No absolute bug-free
or competition-phone sign-off is claimed by the automated results.

BEFORE UPDATE
1. Export your current full JSON backup in the existing browser/PWA. Confirm the
   downloaded file exists in Files/Downloads, outside the app's internal copies.
   If still on v3.8.6 without a visible export button, use the previously supplied
   WIND_SIGHT_PRS_v3.8.6_Backup_Only.zip first. Do not deploy a failed intermediate
   build merely to access a backup button.
2. Deploy all extracted root files to the existing app. Keep the same hostname,
   browser/PWA and data context. Do not clear storage, uninstall or blindly roll
   back to an older version that may not understand new draft fields.
3. Load online, close old app windows/tabs after the update has downloaded, reopen
   and verify v3.8.10 in the header. Do not force updates by deleting website data.
4. Check saved profiles, calibration, matches and DATA status. Test a complete
   workflow and online/offline edit-save-close-reopen on the competition phone.

Invalid edits are recoverable drafts and cannot silently replace valid calibration
or a wind call. DATA may show rejected original copies for investigation; those
are labeled REVIEW ONLY and are not verified restorable backups.
Review legacy observations and suspect calibration axes against original readings:
software cannot reconstruct values already corrupted by older releases.

Preserved: manual wind, Kestrel MIN/MAX, WIND HIT, CALL CURVE, Range Card, profiles,
match workflows, protected storage and ZealTech branding. Calypso/Bluefy remain
absent from production. No DNS/hosting changes are required by this patch itself.

Integrity: SHA256SUMS.txt covers the delivered root files except the checksum
file itself. No test harness or seeded test data is present in the application.
