WIND SIGHT PRS v3.0.4 — Profile Workflow Fix

Field-test fixes:
1. Target DOF Validation Wind Direction is automatically calculated from Stage WIND FROM minus DOF.
2. WD renamed to Wind Direction.
3. Saved profiles have direct module navigation: Rifle, Ammo, Wind Speed, Wind Angle, Target DOF, Review.
4. Calibration data remains editable after entry.
5. Validation runs automatically when NEXT is pressed; manual Validate buttons removed.
6. Target DOF values and holds now commit correctly from the mobile card layout.
7. Review Combo shows Rifle + Ammo together in one combined section.
8. Barrel twist persistence corrected; 1: prefix is no longer re-inserted into the editable value.
9. SAVE & EXIT is available on every profile setup page.
10. Rifle-only incomplete setups are visible/resumable under Profiles.

JavaScript syntax: PASS
